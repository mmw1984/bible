{
  "installCommand": "git clone --depth 1 -b stable https://github.com/flutter/flutter.git $HOME/.flutter-sdk && export PATH=$HOME/.flutter-sdk/bin:$PATH && flutter config --no-analytics && flutter pub get",
  "buildCommand": "export PATH=$HOME/.flutter-sdk/bin:$PATH && flutter build web --release",
  "outputDirectory": "build/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
